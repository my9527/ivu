

import {VARS} from '../utils/vars'

const initState = {
	isLogin: false
}

const HomeReducer = (state = initState, action)=>{

	switch(action.type) {
		

		default:{
			return state;
		}
	}

}

export default HomeReducer;